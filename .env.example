# App runtime (do not put secrets in repo)
NODE_ENV=development

# Hosted app DB (Task 2 will wire actual usage)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/manual_backup_beta

# Required later for crypto (Task 14). 32-byte base64url string.
APP_MASTER_KEY_V1=
