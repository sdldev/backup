{
  "name": "@backup-saas/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "bun src/db/seed.ts",
    "db:seed:dev": "bun src/db/dev-seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@backup-saas/shared": "workspace:*",
    "drizzle-orm": "latest",
    "elysia": "^1.4.0",
    "postgres": "latest"
  },
  "devDependencies": {
    "drizzle-kit": "latest"
  }
}
